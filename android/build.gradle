buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.3.1' // Latest compatible Gradle plugin version
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.7.21' // Update Kotlin plugin to latest compatible version
        classpath 'org.gradle:dev.flutter.flutter-gradle-plugin:2.5.2' // Update plugin and specify compatible version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
